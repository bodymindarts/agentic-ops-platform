server:
  port: 4200
  host: "0.0.0.0"

# Uncomment to enable GitHub OAuth authentication.
# GITHUB_CLIENT_SECRET must be set as an environment variable.
# auth:
#   github_client_id: "your-client-id"
#   github_redirect_uri: "http://localhost:4200/auth/callback"
#   required_org: "bodymindarts"
#   required_team: "ops-team"
#   session_ttl_hours: 24
